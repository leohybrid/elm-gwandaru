/* =========================================================================
   Elm Gwandaru — assets/css/custom.css
   Global animated background + sidebar button styles
   ========================================================================= */

/* ---------- Variables ---------- */
:root {
  --elm-topbar-height: 64px;
  --sidebar-width: 240px;
  --muted-gray: #bfbfbf;
  --accent-muted: rgba(255,255,255,0.06);
  --body-bg: transparent;
  --post-bg: transparent;
  --card-bg: transparent;
  --panel-bg: transparent;
}

/* ---------- Page Offsets ---------- */
html, body {
  background: var(--body-bg) !important;
  background-color: transparent !important;
  margin: 0 !important;
  padding-top: var(--elm-topbar-height);
  color: #eaeaea;
  -webkit-font-smoothing: antialiased;
}

/* ---------- Global Background Layer ---------- */
#elm-background {
  position: fixed;
  inset: 0;
  z-index: -1;
  pointer-events: none;
  overflow: hidden;
  background: linear-gradient(
    to bottom,
    #03040a 0%,
    #07102a 28%,
    #111018 50%,
    #2b1f12 78%,
    #4a3210 100%
  );
  background-attachment: scroll !important;
}

/* ---------- Starfield (top) ---------- */
#elm-background::before {
  content: "";
  position: absolute;
  left: 0; right: 0; top: 0;
  height: 42%;
  background:
    radial-gradient(1.8px 1.8px at 10% 25%, rgba(255,255,255,0.95) 0, transparent 45%),
    radial-gradient(1.2px 1.2px at 45% 12%, rgba(230,230,255,0.85) 0, transparent 45%),
    radial-gradient(1.6px 1.6px at 78% 30%, rgba(200,225,255,0.85) 0, transparent 45%),
    radial-gradient(1.0px 1.0px at 33% 40%, rgba(255,250,210,0.8) 0, transparent 45%),
    radial-gradient(0.9px 0.9px at 88% 12%, rgba(240,240,255,0.85) 0, transparent 45%);
  background-repeat: no-repeat;
  background-size: cover;
  opacity: 0.92;
  mix-blend-mode: screen;
  animation: star-twinkle 6s linear infinite;
}

@keyframes star-twinkle {
  0% { opacity: 0.86; transform: translateY(0); }
  50% { opacity: 1; transform: translateY(-1px); }
  100% { opacity: 0.86; transform: translateY(0); }
}

/* ---------- Shooting Stars ---------- */
#elm-background .shooting-star {
  position: absolute;
  width: 2px; height: 2px;
  background: linear-gradient(90deg, rgba(255,255,255,0.95), rgba(255,255,255,0));
  filter: blur(.3px) drop-shadow(0 0 6px rgba(255,255,255,0.18));
  opacity: 0;
  transform: rotate(-18deg);
  pointer-events: none;
}

#elm-background .shooting-star:nth-of-type(1) { top: 8%; left: -24%; animation: star-shoot 3.8s linear 0.4s infinite; }
#elm-background .shooting-star:nth-of-type(2) { top: 18%; left: -30%; animation: star-shoot 4.9s linear 1.8s infinite; }
#elm-background .shooting-star:nth-of-type(3) { top: 6%; left: -12%; animation: star-shoot 5.6s linear 1.1s infinite; }

@keyframes star-shoot {
  0% { opacity: 0; transform: translate(0,0) rotate(-18deg) scaleX(1); }
  8% { opacity: 1; }
  40% { opacity: 0.85; }
  100% { opacity: 0; transform: translate(160vw, 90vh) rotate(-18deg) scaleX(0.2); }
}

/* ---------- Flower of Life (middle) ---------- */
#elm-background .flower-of-life {
  position: absolute;
  left: 0; right: 0; top: 28%; height: 36%;
  opacity: 0.08;
  pointer-events: none;
  background-image:
    radial-gradient(circle at 30px 30px, rgba(255,255,255,0.18) 0 12px, transparent 13px),
    radial-gradient(circle at 90px 30px, rgba(255,255,255,0.18) 0 12px, transparent 13px);
  background-size: 120px 104px;
  mix-blend-mode: screen;
  filter: blur(0.6px) contrast(.98);
  animation: flower-float 14s linear infinite alternate;
}

@keyframes flower-float {
  0% { transform: translateY(0) scale(1); opacity: 0.07; }
  100% { transform: translateY(-6px) scale(1.02); opacity: 0.11; }
}

/* ---------- Sand Layer (bottom) ---------- */
#elm-background .sand-layer {
  position: absolute;
  left: 0; right: 0; bottom: 0; height: 28%;
  background:
    linear-gradient(180deg, rgba(50,38,24,0.02) 0%, rgba(64,44,22,0.06) 40%, rgba(88,52,18,0.10) 100%),
    radial-gradient(1200px 200px at 10% 60%, rgba(255,235,185,0.03), transparent 20%),
    radial-gradient(1000px 150px at 90% 70%, rgba(255,230,150,0.03), transparent 20%);
  pointer-events: none;
  mix-blend-mode: multiply;
  animation: sand-flow 60s linear infinite;
  opacity: 0.95;
}

@keyframes sand-flow {
  0% { background-position: 0 0, 0 0, 0 0; }
  100% { background-position: 1000px 0, -800px 0, 1200px 0; }
}

/* ---------- Pyramids ---------- */
#elm-background .pyramid {
  position: absolute;
  bottom: 6px;
  width: 0; height: 0;
  border-left: 36px solid transparent;
  border-right: 36px solid transparent;
  border-bottom: 48px solid rgba(243,208,116,0.95);
  filter: drop-shadow(0 6px 8px rgba(0,0,0,0.35));
  opacity: 0.98;
  pointer-events: none;
}

#elm-background .pyramid:nth-of-type(1) { left: 6%; transform: scale(.92); border-bottom-color: rgba(245,210,125,0.95); }
#elm-background .pyramid:nth-of-type(2) { left: 32%; transform: scale(1.12); border-bottom-color: rgba(240,200,100,0.95); }
#elm-background .pyramid:nth-of-type(3) { right: 6%; transform: scale(.86); border-bottom-color: rgba(235,190,95,0.95); }

#elm-background .pyramid::after {
  content: "";
  position: absolute;
  left: -36px; top: -48px;
  width: 72px; height: 48px;
  background: linear-gradient(90deg, rgba(255,255,255,0.08), rgba(0,0,0,0.0));
  transform-origin: center;
  opacity: 0.35;
  pointer-events: none;
}

/* ---------- Sidebar + Buttons ---------- */
#sidebar {
  position: fixed;
  top: var(--elm-topbar-height);
  left: 0; bottom: 0;
  width: var(--sidebar-width);
  padding: 10px 8px;
  display: flex;
  flex-direction: column;
  gap: 6px;
  box-sizing: border-box;
  z-index: 900;
  background: var(--body-bg) !important;
}

.sidebar-nav {
  flex-grow: 1;
  overflow-y: auto;
  padding: 6px 8px 12px;
  margin: 0;
  list-style: none;
}

#sidebar .sidebar-nav .sidebar-link {
  display: block;
  padding: 8px 10px;
  margin: 6px 6px;
  text-align: center;
  background: rgba(11,11,11,0.34) !important;
  color: var(--muted-gray) !important;
  border-radius: 8px;
  border: 1px solid rgba(120,120,120,0.12);
  font-weight: 600;
  font-size: 0.95rem;
  text-decoration: none;
  transition: transform 0.2s cubic-bezier(.2,.9,.28,1), box-shadow 0.2s, background 0.2s, color 0.16s;
  box-shadow: 0 1px 0 rgba(0,0,0,0.25);
}

#sidebar .sidebar-nav .sidebar-link:hover,
#sidebar .sidebar-nav .sidebar-link:focus {
  transform: translateY(-6px) scale(1.07) !important;
  color: #fff !important;
  background: rgba(22,22,22,0.60) !important;
  border-color: rgba(160,160,160,0.14) !important;
  box-shadow: 0 12px 30px rgba(3,3,3,0.48) !important;
  outline: none !important;
}

#sidebar .sidebar-nav .sidebar-link.active {
  background: rgba(14,14,14,0.68) !important;
  border-color: rgba(200,200,200,0.09);
  color: #fff !important;
  transform: translateY(-4px) !important;
}

/* ---------- Hide Topbar on Home ---------- */
.home #topbar, .home .site-topbar, .home .navbar {
  display: none !important;
}

/* ---------- Hide Footer ---------- */
#footer, .site-footer {
  display: none !important;
}

/* ---------- Transparent Content ---------- */
:is(html, body, main, .container, .container-fluid, .site-main, .page-main, #main-wrapper,
   article, .post, .post.px-1, .page, .post-content, .content, .content.post-content,
   .post-tail-wrapper, .card, .card-body, .panel, .panel-body, .post-meta, .post-desc,
   .post-title, .preview-img, .poetry_list, .category, .archive, .listing,
   #panel-wrapper, aside, .access, .post-content-wrapper, .post-wrapper, .post-body, .content-wrapper,
   .main, .layout-post, .main-content, .post-content-main, .row, .col, .col-12, .col-md-8) {
  background: var(--post-bg) !important;
  background-color: transparent !important;
  box-shadow: none !important;
  backdrop-filter: none !important;
  position: relative;
  z-index: 2 !important;
}

/* ---------- Post-Specific Transparency ---------- */
body.post-type-post #main-wrapper,
body.post-type-post .container,
body.post-type-post .page-content,
body.post-type-post .post.px-1,
body.post-type-post .content.post-content,
body.post-type-post article,
body.post-type-post .post-content,
body.post-type-post .content-wrapper,
body.post-type-post .post-body,
body.post-type-post .main,
body.post-type-post .layout-post,
body.post-type-post .main-content,
body.post-type-post .post-content-main,
body.post-type-post .row,
body.post-type-post .col,
body.post-type-post .col-12,
body.post-type-post .col-md-8 {
  background: var(--post-bg) !important;
  background-color: transparent !important;
  z-index: 2 !important;
}

/* ---------- Fallback for Any White Background ---------- */
[class*="post"], [class*="page"], [class*="content"], [class*="wrapper"], [class*="main"], [class*="layout"], [class*="col"], [class*="row"] {
  background: var(--post-bg) !important;
  background-color: transparent !important;
}

/* ---------- Poem Styling ---------- */
.post-type-post p {
  color: #eaeaea;
  font-size: 1.1rem;
  line-height: 1.6;
  margin: 0.5em 0;
  text-align: center;
}

/* ---------- Text and Headings ---------- */
.post-title, h1, h2, h3 {
  color: #eaeaea;
}

/* ---------- Small Screens ---------- */
@media (max-width: 768px) {
  #sidebar { width: 64px; }
  #sidebar .sidebar-nav .sidebar-link { padding: 6px; font-size: 0.86rem; }
  #elm-background .pyramid { display: none; }
  #elm-background .flower-of-life { opacity: 0.06; }
}

/* ---------- Accessibility ---------- */
#sidebar .sidebar-nav .sidebar-link:focus {
  outline: 3px solid var(--accent-muted) !important;
  outline-offset: 3px;
}